import PropTypes from 'prop-types';
import React, { Component } from 'react';
import CheckButton from './CheckButton';

class Image extemds React.Component {
  constructor(props) {
    super(props);
  }
}
